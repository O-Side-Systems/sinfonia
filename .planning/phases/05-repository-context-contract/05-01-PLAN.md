---
phase: 05-repository-context-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/CONTEXT-CONTRACT.md
autonomous: true
requirements: [CTXGRAPH-03]
must_haves:
  truths:
    - "docs/CONTEXT-CONTRACT.md exists as the standalone contract (the map, per D-01) and defines the doc-graph node shape, front-matter schema, resolution semantics, sizing rules, and read/write protocol (SC-1, D-01)"
    - "The contract specifies all six front-matter fields: title, version, status, owners, last_verified_sha, derived_from (SC-1)"
    - "The contract documents nearest-wins AGENTS.md resolution, one-concern/one-screen sizing, the JIT read protocol, and the write protocol (diffs ride the code PR under CODEOWNERS, version bump, no autonomous mutation per DEC-004) (SC-1)"
    - "The contract carries a manual Conformance Checklist that downstream nodes and templates are validated against (SC-3 manual half)"
  artifacts:
    - path: "docs/CONTEXT-CONTRACT.md"
      provides: "The Repository Context Contract: node shape + front-matter schema + resolution algorithm + read/write protocol + conformance checklist"
      contains: "## Conformance Checklist"
  key_links:
    - from: "docs/CONTEXT-CONTRACT.md"
      to: "docs/HARNESS-SPEC.md"
      via: "Companion specs header + back-references in normative-language style"
      pattern: "HARNESS-SPEC"
    - from: "docs/CONTEXT-CONTRACT.md §Conformance Checklist"
      to: ".github/CODEOWNERS"
      via: "checklist item requiring node owners/path-globs to match CODEOWNERS"
      pattern: "CODEOWNERS"
---

<objective>
Author `docs/CONTEXT-CONTRACT.md` — the standalone Repository Context Contract (the "map"), per D-01. This is the upstream artifact for the entire phase: it defines the node shape, front-matter schema, nearest-wins resolution, one-concern/one-screen sizing, the read protocol, the write protocol, and a manual Conformance Checklist that every downstream dogfood node and template is validated against.

Purpose: Establish the single normative spec for the doc-graph (CTXGRAPH-03 / SC-1, SC-3 manual half). Per DEC-004, this separates "the map" (this contract) from "the sensor" (HARNESS-SPEC). The contract is the definition only — it contains NO concrete node data (no real paths, no owners, no crate-specific tables).

Output: `docs/CONTEXT-CONTRACT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-repository-context-contract/05-CONTEXT.md
@.planning/phases/05-repository-context-contract/05-RESEARCH.md
@.planning/phases/05-repository-context-contract/05-PATTERNS.md
@.planning/phases/05-repository-context-contract/05-VALIDATION.md

# Structural style analog — mirror its header block, RFC 2119 normative-language
# section, §-numbered sections, REQUIRED/RECOMMENDED tagging, two-column
# comparison tables, and §9-style conformance checklist.
@docs/HARNESS-SPEC.md

<interfaces>
<!-- The six required front-matter fields the contract MUST define (RESEARCH.md Pattern 1).
     This is the authoritative schema — every downstream node/template conforms to it. -->
Front-matter fields (in this order):
  title              — human label (string)
  version            — semver string, bumped on every node diff
  status             — one of: active | draft | deprecated
  owners             — YAML array of CODEOWNERS-style handles, e.g. ["@osidesys", "@leebrett"]
  last_verified_sha  — 7-char git SHA present in main ancestry; Phase-6 stale-linter hook
  derived_from       — path/URL of the authoritative source the node distils
"See also" — a body bullet list (NOT front-matter) of cross-links to related nodes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/CONTEXT-CONTRACT.md (node shape, protocols, conformance checklist)</name>
  <files>docs/CONTEXT-CONTRACT.md</files>
  <read_first>
    - docs/HARNESS-SPEC.md (lines 1–96 for the header + Normative Language + §3 Conformance-overview style; lines 329–339 for §7.3 bullet style; lines 416–441 for the §9 checklist `- [ ]` bullet style)
    - .planning/phases/05-repository-context-contract/05-RESEARCH.md (Pattern 1 front-matter schema, Pattern 2 nearest-wins algorithm, Pattern 3 sizing, Pattern 4 write protocol, Pattern 5 CODEOWNERS mapping, §Manual Conformance Checklist at lines 500–518)
    - .planning/phases/05-repository-context-contract/05-PATTERNS.md (the `docs/CONTEXT-CONTRACT.md` section, lines 31–93 — doc-header / numbered-section / conformance-checklist / two-column-table / non-normative-appendix patterns)
    - .planning/phases/05-repository-context-contract/05-CONTEXT.md (D-01 contract scope; D-08 ownership-table mirrors CODEOWNERS; boundary note: last_verified_sha must anticipate the Phase-6 stale linter; DEC-004 no autonomous mutation)
  </read_first>
  <action>
    Create `docs/CONTEXT-CONTRACT.md` mirroring the HARNESS-SPEC structural style (bullet-header block with Status/Audience/Companion specs, then a Normative Language section using RFC 2119 caps, then `## N. Section` numbered sections with REQUIRED/RECOMMENDED qualifiers in headings and `(§N.M)` back-references).

    The contract MUST cover, in numbered sections:
    1. Purpose & scope — references DEC-004 "separate the sensor (HARNESS-SPEC) from the map (this contract)"; state explicitly that this doc is the spec/shape only and contains NO concrete node data (per Pitfall 3).
    2. Node front-matter schema — define the six required fields in the order title, version, status, owners, last_verified_sha, derived_from, with the per-field semantics from RESEARCH Pattern 1. State status ∈ {active, draft, deprecated}. State that last_verified_sha is a 7-char SHA present in main ancestry and is the hook the Phase-6 stale linter will consume (do NOT build the linter — boundary note). Define "See also" as a body bullet list (not front-matter).
    3. Nearest-wins resolution — the root→leaf walk algorithm from RESEARCH Pattern 2 (read root AGENTS.md always; read each AGENTS.md on the path to the edited file; more-specific overrides less-specific; this is NOT a recursive traversal of all nodes; follow links only when acceptance criteria implicate that path-glob).
    4. One-concern / one-screen sizing — hard limit ≤ 50 rendered body lines (excluding front-matter), one concern per node; root node is links + module-ownership table, NOT prose (Pitfall 1).
    5. Read protocol — the JIT order: root AGENTS.md → acceptance-matching links only → grep/glob the rest → never blanket-read docs/. (This is the prose spec the WORKFLOW prompts in Plan 04 point at.)
    6. Write protocol — node diffs ride the code PR under CODEOWNERS; version bump on every change; set last_verified_sha from `git rev-parse --short HEAD` at merge time (RESEARCH Pattern 4 + Pitfall 5 guidance); NO autonomous mutation — an agent that finds a stale node proposes a draft diff and stops (DEC-004). State the module-ownership table in each AGENTS.md mirrors `.github/CODEOWNERS`; if they diverge, CODEOWNERS is authoritative (RESEARCH Pattern 5).
    7. Conformance Checklist (`## Conformance Checklist`) — copy the nine `- [ ]` items from RESEARCH §Manual Conformance Checklist (lines 502–518) verbatim in structure: six front-matter fields present; status valid enum; owners match a CODEOWNERS entry; last_verified_sha is a 7-char SHA in main; body ≤ 50 lines; one concern; "See also" present; no duplication of linked spec content; every module-ownership path-glob matches a `.github/CODEOWNERS` pattern.

    Use a two-column "Prescribed (this contract) | NOT prescribed (node author's choice)" table where contrasting mandate vs. discretion. Keep illustrative examples in a `## N. ... (non-normative)` appendix if used. Do NOT paste real owner handles or real crate paths into the body — those belong in the dogfood nodes (Plan 02).
  </action>
  <verify>
    <automated>test -f docs/CONTEXT-CONTRACT.md && grep -q "## Conformance Checklist" docs/CONTEXT-CONTRACT.md && for f in title version status owners last_verified_sha derived_from; do grep -q "$f" docs/CONTEXT-CONTRACT.md || { echo "MISSING field def: $f"; exit 1; }; done && grep -q "RFC 2119" docs/CONTEXT-CONTRACT.md && grep -qi "no autonomous mutation\|MUST NOT.*commit\|MUST NOT.*push" docs/CONTEXT-CONTRACT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `ls docs/CONTEXT-CONTRACT.md` succeeds.
    - `grep -c '^- \[ \]' docs/CONTEXT-CONTRACT.md` ≥ 9 (the nine conformance-checklist items).
    - All six front-matter field names (title, version, status, owners, last_verified_sha, derived_from) appear as defined fields in the schema section.
    - Document contains an RFC 2119 normative-language section and `## N.`-numbered sections in HARNESS-SPEC style.
    - The write-protocol section states explicitly: diffs ride the code PR under CODEOWNERS, version bump, no autonomous mutation (DEC-004).
    - The body contains NO real owner handles paired with real crate path-globs in a populated module-ownership table (it is a spec, not an instance — Pitfall 3); a placeholder/illustrative example in a non-normative appendix is acceptable.
  </acceptance_criteria>
  <done>
    `docs/CONTEXT-CONTRACT.md` exists, defines the six-field front-matter schema, nearest-wins resolution, one-screen sizing, read protocol, write protocol (DEC-004), and a 9-item manual Conformance Checklist — all in HARNESS-SPEC structural style, with no concrete node data in the body.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Phase 5 introduces no code, no external inputs, no auth, no data flows — documentation authoring only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | docs/CONTEXT-CONTRACT.md (the normative spec) | mitigate | Doc-diff rides the code PR; existing CODEOWNERS `* @osidesys @leebrett` catch-all already gates `docs/` edits with required human review (DEC-004 enforcement). |
| T-05-02 | (S/R/I/D/E) | n/a | accept | No code, inputs, auth, or data flows in this plan; per RESEARCH §Security Domain ASVS V2–V6 do not apply. No high-severity threats. |

No package-manager installs in this plan (no Package Legitimacy Gate needed).
</threat_model>

<verification>
- `test -f docs/CONTEXT-CONTRACT.md` (contract exists)
- `grep -c 'CONTEXT-CONTRACT' docs/HARNESS-SPEC.md` is handled in Plan 04 (the §7/§9 reference), not here.
- Manual: walk the §Conformance Checklist text and confirm each of the nine items is present and unambiguous enough to validate a node against.
</verification>

<success_criteria>
- SC-1 (contract specifies node front-matter, nearest-wins semantics, one-concern/one-screen sizing, read/write protocol): satisfied by Task 1.
- SC-3 manual half (conformance checklist line) — the checklist itself lives here; the HARNESS-SPEC §9 reference line is Plan 04.
</success_criteria>

<output>
Create `.planning/phases/05-repository-context-contract/05-01-SUMMARY.md` when done.
</output>
