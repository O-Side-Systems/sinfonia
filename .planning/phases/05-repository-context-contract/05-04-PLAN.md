---
phase: 05-repository-context-contract
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - docs/HARNESS-SPEC.md
  - docker/WORKFLOW.md
  - docker/WORKFLOW.example.md
autonomous: true
requirements: [CTXGRAPH-03, CTXGRAPH-02]
must_haves:
  truths:
    - "HARNESS-SPEC §7.3 lists the Context Contract as a REQUIRED convention referencing docs/CONTEXT-CONTRACT.md (D-02, SC-3)"
    - "HARNESS-SPEC §9 conformance checklist has a context-graph line (root AGENTS.md conforms + CODEOWNERS gates **/AGENTS.md) (D-02, SC-3)"
    - "Every state prompt (Todo, In Progress, fallback) in both docker/WORKFLOW.md and docker/WORKFLOW.example.md uses the JIT read protocol: the fallback Orient step is rewritten to the full protocol (root AGENTS.md → acceptance-matching links → grep/glob → never blanket-read docs/), and the Todo + In Progress prompts each carry a one-sentence JIT orient preamble pointing to root AGENTS.md (D-04, SC-4 / CTXGRAPH-02)"
    - "No blanket 'Orient: ... docs/' slurp and no CLAUDE.md reference remain in either WORKFLOW file; the stale CLAUDE.md reference is dropped (D-03, D-04, SC-4)"
    - "In Review is named in D-04 but is intentionally not an active state and ships no agent prompt — honoring D-04 means covering every state prompt that actually exists (Todo, In Progress, fallback) in both files"
  artifacts:
    - path: "docs/HARNESS-SPEC.md"
      provides: "§7.3 Context-graph convention bullet + §9 conformance-checklist line referencing the contract"
      contains: "CONTEXT-CONTRACT"
    - path: "docker/WORKFLOW.md"
      provides: "JIT orient protocol in the fallback prompt + JIT orient preamble in the Todo and In Progress prompts"
      contains: "AGENTS.md"
    - path: "docker/WORKFLOW.example.md"
      provides: "JIT orient protocol in the fallback prompt + JIT orient preamble in the Todo and In Progress prompts (parity with WORKFLOW.md)"
      contains: "AGENTS.md"
  key_links:
    - from: "docs/HARNESS-SPEC.md §7.3"
      to: "docs/CONTEXT-CONTRACT.md"
      via: "markdown link in the new Context-graph convention bullet"
      pattern: "CONTEXT-CONTRACT.md"
    - from: "docker/WORKFLOW.md + docker/WORKFLOW.example.md every state prompt (Todo, In Progress, fallback Orient step)"
      to: "AGENTS.md (root entry point)"
      via: "JIT read protocol / preamble points agents to root AGENTS.md first"
      pattern: "root .?AGENTS.md"
---

<objective>
Wire the contract into the spec and switch EVERY state prompt to the JIT read protocol:
1. HARNESS-SPEC §7.3 gets a Context-graph REQUIRED convention bullet referencing `docs/CONTEXT-CONTRACT.md`, and §9 gets a matching conformance-checklist line (D-02). The contract body stays in `docs/CONTEXT-CONTRACT.md` — HARNESS-SPEC only points to it.
2. The JIT read protocol is applied to ALL state prompts in BOTH `docker/WORKFLOW.md` and `docker/WORKFLOW.example.md` per the controlling locked decision D-04:
   - The fallback "What to do" Orient step (`docker/WORKFLOW.md:499` / `docker/WORKFLOW.example.md:737`) is rewritten from the blanket `README.md, CLAUDE.md, docs/` slurp to the full lettered JIT protocol, dropping the stale `CLAUDE.md` reference (D-03).
   - The Todo prompt and the In Progress prompt in each file gain a one-sentence JIT orient preamble (a positive direction to use the protocol), inserted before STEP 0 so the existing STEP 0 scripting is untouched.
   - "In Review" is named in D-04 but is intentionally NOT an active state in either WORKFLOW file (it is the human review gate and ships no agent prompt) — there is no In Review prompt to edit, so honoring D-04 means covering every state prompt that exists: Todo, In Progress, and the fallback.

Purpose: Satisfy CTXGRAPH-03 / SC-3 (§7 lists the convention + checklist line) and CTXGRAPH-02 / SC-4 (every state prompt uses the JIT protocol, no blanket docs/ read) — honoring locked decision D-04.

Output: edits to `docs/HARNESS-SPEC.md`, `docker/WORKFLOW.md`, `docker/WORKFLOW.example.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-repository-context-contract/05-CONTEXT.md
@.planning/phases/05-repository-context-contract/05-RESEARCH.md
@.planning/phases/05-repository-context-contract/05-PATTERNS.md

# The contract being referenced (must exist — Plan 01) and the root entry point the JIT
# protocol points to (must exist — Plan 02).
@docs/CONTEXT-CONTRACT.md
@AGENTS.md

# Self-analog files being edited in place.
@docs/HARNESS-SPEC.md
@docker/WORKFLOW.md
@docker/WORKFLOW.example.md

<interfaces>
<!-- Exact insertion points (verified against current files) -->
HARNESS-SPEC.md:
  §7.3 "Repository conventions (REQUIRED)" — header at line 328; existing CI-gates bullet
       ends at line 339. INSERT the new Context-graph bullet AFTER line 339 (before §7.4 at 341).
  §9 "Conformance Checklist" — header at line 416; the `*(RECOMMENDED)*` line is at 438.
       INSERT the new context-graph `- [ ]` line BEFORE line 438 (with the REQUIRED items).

WORKFLOW.md state prompts (the YAML `prompts:` map — exactly two `prompt:` entries: Todo, In Progress):
  - "Todo" prompt begins at line 73; `prompt: |` at line 76; intro line "You are picking up
    Linear issue ..." at line 77; first work section "## Issue description" at line 79; STEP 0
    at line 83. INSERT the JIT orient preamble between the intro line (77) and "## Issue
    description" (79), as its own paragraph.
  - "In Progress" prompt begins at line 294; `prompt: |` at line 298; intro line "Resume work
    on ..." at line 299; STEP 0 at line 301. INSERT the JIT orient preamble between the intro
    line (299) and STEP 0 (301), as its own paragraph.
  - Fallback "What to do" — the only "Orient:" slurp is line 499 (step 1 of the numbered list).
    "In Review" is listed in the state machine comments (line 71) as a NON-active human gate and
    has NO `prompt:` entry — there is nothing to edit for it. (Confirmed: only two `prompt: |`
    keys exist in this file, at lines 76 and 298.)

WORKFLOW.example.md state prompts (parity with WORKFLOW.md — exactly two `prompt:` entries: Todo, In Progress):
  - "Todo" prompt: `prompt: |` at line 94; intro line "You are picking up Linear issue ..." at
    line 95; first work section "## Issue description" at line 97; STEP 0 at line 101. INSERT the
    JIT orient preamble between the intro line (95) and "## Issue description" (97), as its own paragraph.
  - "In Progress" prompt: begins at line 319; `prompt: |` at line 323; intro line "Resume work
    on ..." at line 324; STEP 0 at line 326. INSERT the JIT orient preamble between the intro
    line (324) and STEP 0 (326), as its own paragraph.
  - Fallback "What to do" — the only "Orient:" slurp is line 737 (step 1). No In Review prompt.
    (Confirmed: only two `prompt: |` keys exist in this file, at lines 94 and 323.)

Both fallback "Orient:" lines currently read exactly:
  `1. Orient: ` + backtick `README.md`, `CLAUDE.md`, `docs/`.
Items 2–6 in each fallback numbered list are UNCHANGED.

JIT replacement substance (RESEARCH §Code Examples + §Pattern 6; exact wording = executor's
discretion, but MUST be):
  - Fallback (full protocol): read root AGENTS.md first → match module-ownership path-globs to the
    issue's acceptance criteria → read ONLY linked nodes that match → grep/glob the rest
    (`grep -r '<term>' crates/` / `git ls-files | grep`) → do NOT open docs/ wholesale, do NOT read
    README.md unless a node links to it. NO CLAUDE.md reference.
  - Todo + In Progress (one-sentence preamble): e.g. "**Orient:** start at the repo-root
    `AGENTS.md` and follow only the links relevant to this issue's acceptance criteria; grep/glob
    for anything else — do not bulk-read `docs/`." NO CLAUDE.md reference. Use identical preamble
    text across both prompts and both files for parity.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend HARNESS-SPEC §7.3 convention bullet + §9 checklist line</name>
  <files>docs/HARNESS-SPEC.md</files>
  <read_first>
    - docs/HARNESS-SPEC.md (lines 328–346 for §7.3 bullet style + the §7.4 boundary; lines 416–442 for the §9 `- [ ]` checklist style and where REQUIRED items end / RECOMMENDED begins)
    - .planning/phases/05-repository-context-contract/05-RESEARCH.md (Pattern 7 — exact §7.3 bullet text and §9 line text)
    - .planning/phases/05-repository-context-contract/05-PATTERNS.md (HARNESS-SPEC amendment section — insertion points + bold-term / MUST-caps / parenthetical-back-reference conventions)
    - docs/CONTEXT-CONTRACT.md (confirm the link target path is correct)
  </read_first>
  <action>
    Edit `docs/HARNESS-SPEC.md` (D-02 — reference only, NOT the contract body):

    - In §7.3 (after the CI-gates bullet ending at line 339, before §7.4 at line 341), insert a new bullet: `- **Context graph:** The repo MUST maintain a hierarchical `AGENTS.md` doc-graph conforming to `docs/CONTEXT-CONTRACT.md`. The root `AGENTS.md` is the agent entry point; all node edits ride in the code PR under CODEOWNERS. See [`docs/CONTEXT-CONTRACT.md`](docs/CONTEXT-CONTRACT.md) for the full contract.` Match the existing bullet style: bold key term, MUST in caps, markdown link.
    - In §9 (before the `*(RECOMMENDED)*` line at 438, with the REQUIRED items), append: `- [ ] A root `AGENTS.md` exists and conforms to `docs/CONTEXT-CONTRACT.md`; CODEOWNERS gates all `**/AGENTS.md` edits. (§7.3)` — `- [ ]` bullet, `(§7.3)` back-reference, consistent with existing checklist lines.

    Exact wording is at the executor's discretion within these bounds; the contract body must NOT be duplicated into HARNESS-SPEC (D-01/D-02).
  </action>
  <verify>
    <automated>[ "$(grep -c 'CONTEXT-CONTRACT' docs/HARNESS-SPEC.md)" -ge 2 ] && grep -q 'Context graph' docs/HARNESS-SPEC.md && grep -q '\*\*/AGENTS.md' docs/HARNESS-SPEC.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'CONTEXT-CONTRACT' docs/HARNESS-SPEC.md` ≥ 2 (the §7.3 bullet's prose mention + markdown link, and/or the §9 line).
    - The §7.3 Context-graph bullet appears between the CI-gates bullet and the §7.4 header, uses bold term + MUST, and markdown-links to `docs/CONTEXT-CONTRACT.md`.
    - The §9 checklist gains a `- [ ]` line referencing root `AGENTS.md` conformance + `**/AGENTS.md` CODEOWNERS gating, placed with the REQUIRED items (before the `*(RECOMMENDED)*` line), with a `(§7.3)` back-reference.
    - HARNESS-SPEC does NOT contain the contract body (no front-matter schema definition, no resolution algorithm prose copied in) — reference only.
  </acceptance_criteria>
  <done>
    HARNESS-SPEC §7.3 lists the Context Contract as a REQUIRED convention referencing `docs/CONTEXT-CONTRACT.md`, and §9 has the matching conformance-checklist line (SC-3).
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply the JIT read protocol to every state prompt in both WORKFLOW files</name>
  <files>docker/WORKFLOW.md, docker/WORKFLOW.example.md</files>
  <read_first>
    - docker/WORKFLOW.md (lines 73–103 — the "Todo" prompt intro + STEP 0 boundary; lines 294–307 — the "In Progress" prompt intro + STEP 0 boundary; lines 480–505 — the fallback "What to do" block, slurp at line 499, items 2–6 must stay unchanged; line 71 confirms In Review is a non-active human gate with no prompt)
    - docker/WORKFLOW.example.md (lines 94–105 — "Todo" prompt; lines 319–330 — "In Progress" prompt; lines 720–743 — fallback block, slurp at line 737)
    - .planning/phases/05-repository-context-contract/05-RESEARCH.md (Pattern 6 + §Code Examples — full JIT replacement text for the fallback AND the one-sentence preamble text for Todo/In Progress; Open Question 1 RESOLVED — D-04 requires ALL state prompts; Pitfall 4 — confirm CLAUDE.md appears ONLY at 499/737 before editing)
    - .planning/phases/05-repository-context-contract/05-PATTERNS.md (WORKFLOW fallback edit sections; "Fallback Prompt List Item Style" — lettered a./b./c./d. sub-list + a `Do NOT` warning paragraph)
  </read_first>
  <action>
    Apply the JIT read protocol to EVERY state prompt in BOTH `docker/WORKFLOW.md` and `docker/WORKFLOW.example.md` (D-04 — the controlling locked decision; do NOT edit the fallback only).

    1. Fallback "What to do" (full protocol): In each file (`docker/WORKFLOW.md:499`, `docker/WORKFLOW.example.md:737`), replace the single line `1. Orient: README.md, CLAUDE.md, docs/.` with the full JIT orient protocol as item 1 of the numbered list. Substance (exact wording is the executor's discretion): a lettered sub-list — (a) read root `AGENTS.md`; (b) in its module-ownership table, find rows whose path-globs match files this issue's acceptance criteria mention or imply; (c) read ONLY the `AGENTS.md` nodes linked from those matching rows; (d) for anything not yet found, `grep -r '<term>' crates/` or `git ls-files | grep '<pattern>'` — followed by a `Do NOT` line: do not open `docs/` wholesale, do not read `README.md` unless a node links to it. The replacement MUST drop the `CLAUDE.md` reference entirely (D-03). Items 2–6 of each fallback list are byte-unchanged.

    2. Todo prompt preamble: In each file, insert a one-sentence JIT orient preamble in the "Todo" prompt as its own paragraph — `docker/WORKFLOW.md` between the intro line (line 77, "You are picking up ...") and "## Issue description" (line 79); `docker/WORKFLOW.example.md` between the intro line (line 95) and "## Issue description" (line 97). Place it BEFORE STEP 0 so the STEP 0 bash scripting is untouched (non-disruption constraint, RESEARCH Pattern 6).

    3. In Progress prompt preamble: In each file, insert the same one-sentence JIT orient preamble in the "In Progress" prompt as its own paragraph — `docker/WORKFLOW.md` between the intro line (line 299, "Resume work on ...") and STEP 0 (line 301); `docker/WORKFLOW.example.md` between the intro line (line 324) and STEP 0 (line 326). Same placement rule: before STEP 0, bash scripting untouched.

    Preamble substance (exact wording = executor's discretion, identical across both prompts and both files): "**Orient:** start at the repo-root `AGENTS.md` and follow only the links relevant to this issue's acceptance criteria; grep/glob for anything else — do not bulk-read `docs/`." NO `CLAUDE.md` reference anywhere.

    Do NOT add or edit an In Review prompt — In Review is a non-active human review gate with no `prompt:` entry (WORKFLOW comment line 71; only two `prompt: |` keys exist per file). Honoring D-04 across the prompts that exist (Todo, In Progress, fallback) in both files satisfies "every state prompt uses the JIT protocol." Use identical JIT/preamble text in both files for parity.
  </action>
  <verify>
    <automated>[ "$(grep -c 'Orient.*docs/' docker/WORKFLOW.md docker/WORKFLOW.example.md 2>/dev/null | awk -F: '{s+=$2} END{print s+0}')" -eq 0 ] && [ "$(grep -c 'CLAUDE.md' docker/WORKFLOW.md docker/WORKFLOW.example.md 2>/dev/null | awk -F: '{s+=$2} END{print s+0}')" -eq 0 ] && [ "$(grep -c 'AGENTS.md' docker/WORKFLOW.md)" -ge 3 ] && [ "$(grep -c 'AGENTS.md' docker/WORKFLOW.example.md)" -ge 3 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Orient.*docs/" docker/WORKFLOW.md docker/WORKFLOW.example.md` → 0 hits (no blanket docs/ slurp remains anywhere).
    - `grep -n "CLAUDE.md" docker/WORKFLOW.md docker/WORKFLOW.example.md` → 0 hits (stale reference dropped, D-03).
    - `grep -c "AGENTS.md" docker/WORKFLOW.md` ≥ 3 AND `grep -c "AGENTS.md" docker/WORKFLOW.example.md` ≥ 3 — at least one root-`AGENTS.md` reference per state prompt (Todo preamble, In Progress preamble, fallback protocol) in each file. This count (≥ the number of state prompts = 3 per file) is the JIT-presence-across-all-state-prompts assertion (SC-4 / CTXGRAPH-02 / D-04).
    - The Todo and In Progress prompts in BOTH files each contain a JIT orient preamble pointing at root `AGENTS.md`, inserted before STEP 0; the STEP 0 bash blocks are byte-unchanged.
    - The fallback Orient step in both files uses the lettered a./b./c./d. sub-list + a `Do NOT` warning, protocol order root AGENTS.md → acceptance-matching links → grep/glob → never blanket docs/; fallback items 2–6 are byte-unchanged.
    - The JIT/preamble text is identical across both files (parity).
  </acceptance_criteria>
  <done>
    Every state prompt (Todo, In Progress, fallback) in both WORKFLOW files uses the JIT read protocol pointing at root `AGENTS.md` — the fallback Orient step is the full protocol, the Todo and In Progress prompts each carry the JIT orient preamble — with no blanket `docs/` slurp and no `CLAUDE.md` reference (SC-4 / CTXGRAPH-02 / D-04).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Spec-doc + prompt-text editing only — no code, inputs, auth, or data flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | docker/WORKFLOW*.md (agent-instruction prompts) | mitigate | Prompt edits ride the gated PR under CODEOWNERS; human review confirms the JIT protocol is deterministic before merge (DEC-004). |
| T-05-09 | Information Disclosure | JIT protocol guidance (what agents read) | accept | The JIT protocol REDUCES context surface (narrower reads than the prior blanket docs/ slurp); no new disclosure risk introduced. |
| T-05-10 | (S/R/D/E) | n/a | accept | No external inputs, auth, or data flows; ASVS V2–V6 N/A per RESEARCH §Security Domain. |

No package-manager installs in this plan.
</threat_model>

<verification>
- `grep -c 'CONTEXT-CONTRACT' docs/HARNESS-SPEC.md` ≥ 2.
- `grep -n "Orient.*docs/" docker/WORKFLOW.md docker/WORKFLOW.example.md` → 0 hits.
- `grep -n "CLAUDE.md" docker/WORKFLOW.md docker/WORKFLOW.example.md` → 0 hits.
- `grep -c "AGENTS.md" docker/WORKFLOW.md` ≥ 3 and same for WORKFLOW.example.md (one per state prompt: Todo, In Progress, fallback).
- Manual: read the rewritten fallback Orient step + the Todo/In Progress JIT preambles in both files — every state prompt positively directs the agent to start at root `AGENTS.md` and follow only matching links.
</verification>

<success_criteria>
- SC-3 (§7 lists the Context Contract as a REQUIRED convention + §9 checklist line): satisfied by Task 1.
- SC-4 (every state prompt uses the JIT read protocol, no blanket docs/ read): satisfied by Task 2 — fallback rewritten to the full protocol + Todo and In Progress prompts carry the JIT orient preamble, in both WORKFLOW files (D-04).
- D-02, D-03, D-04: spec wiring + dropped CLAUDE.md + JIT protocol across all state prompts all satisfied.
</success_criteria>

<output>
Create `.planning/phases/05-repository-context-contract/05-04-SUMMARY.md` when done.
</output>
