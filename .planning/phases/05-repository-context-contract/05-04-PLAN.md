---
phase: 05-repository-context-contract
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - docs/HARNESS-SPEC.md
  - docker/WORKFLOW.md
  - docker/WORKFLOW.example.md
autonomous: true
requirements: [CTXGRAPH-03, CTXGRAPH-02]
must_haves:
  truths:
    - "HARNESS-SPEC §7.3 lists the Context Contract as a REQUIRED convention referencing docs/CONTEXT-CONTRACT.md (D-02, SC-3)"
    - "HARNESS-SPEC §9 conformance checklist has a context-graph line (root AGENTS.md conforms + CODEOWNERS gates **/AGENTS.md) (D-02, SC-3)"
    - "The fallback 'What to do' Orient step in both docker/WORKFLOW.md and docker/WORKFLOW.example.md uses the JIT read protocol (root AGENTS.md → acceptance-matching links → grep/glob → never blanket-read docs/) and the stale CLAUDE.md reference is dropped (D-03, D-04, SC-4)"
    - "No blanket 'Orient: ... docs/' slurp and no CLAUDE.md reference remain in either WORKFLOW file (D-03, D-04, SC-4)"
  artifacts:
    - path: "docs/HARNESS-SPEC.md"
      provides: "§7.3 Context-graph convention bullet + §9 conformance-checklist line referencing the contract"
      contains: "CONTEXT-CONTRACT"
    - path: "docker/WORKFLOW.md"
      provides: "JIT orient protocol in the fallback prompt"
      contains: "AGENTS.md"
    - path: "docker/WORKFLOW.example.md"
      provides: "JIT orient protocol in the fallback prompt (parity with WORKFLOW.md)"
      contains: "AGENTS.md"
  key_links:
    - from: "docs/HARNESS-SPEC.md §7.3"
      to: "docs/CONTEXT-CONTRACT.md"
      via: "markdown link in the new Context-graph convention bullet"
      pattern: "CONTEXT-CONTRACT.md"
    - from: "docker/WORKFLOW.md + docker/WORKFLOW.example.md fallback Orient step"
      to: "AGENTS.md (root entry point)"
      via: "JIT read protocol points agents to root AGENTS.md first"
      pattern: "root .?AGENTS.md"
---

<objective>
Wire the contract into the spec and switch the prompts to the JIT read protocol:
1. HARNESS-SPEC §7.3 gets a Context-graph REQUIRED convention bullet referencing `docs/CONTEXT-CONTRACT.md`, and §9 gets a matching conformance-checklist line (D-02). The contract body stays in `docs/CONTEXT-CONTRACT.md` — HARNESS-SPEC only points to it.
2. The fallback "What to do" Orient step in BOTH `docker/WORKFLOW.md` (line 499) and `docker/WORKFLOW.example.md` (line 737) is replaced with the JIT protocol, dropping the stale `CLAUDE.md` reference and the blanket `docs/` slurp (D-03, D-04).

Purpose: Satisfy CTXGRAPH-03 / SC-3 (§7 lists the convention + checklist line) and CTXGRAPH-02 / SC-4 (every state prompt uses the JIT protocol, no blanket docs/ read).

Output: edits to `docs/HARNESS-SPEC.md`, `docker/WORKFLOW.md`, `docker/WORKFLOW.example.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-repository-context-contract/05-CONTEXT.md
@.planning/phases/05-repository-context-contract/05-RESEARCH.md
@.planning/phases/05-repository-context-contract/05-PATTERNS.md

# The contract being referenced (must exist — Plan 01) and the root entry point the JIT
# protocol points to (must exist — Plan 02).
@docs/CONTEXT-CONTRACT.md
@AGENTS.md

# Self-analog files being edited in place.
@docs/HARNESS-SPEC.md
@docker/WORKFLOW.md
@docker/WORKFLOW.example.md

<interfaces>
<!-- Exact insertion points (verified against current files) -->
HARNESS-SPEC.md:
  §7.3 "Repository conventions (REQUIRED)" — header at line 328; existing CI-gates bullet
       ends at line 339. INSERT the new Context-graph bullet AFTER line 339 (before §7.4 at 341).
  §9 "Conformance Checklist" — header at line 416; the `*(RECOMMENDED)*` line is at 438.
       INSERT the new context-graph `- [ ]` line BEFORE line 438 (with the REQUIRED items).

WORKFLOW.md:   the only "Orient:" slurp is line 499 (fallback "What to do" step 1).
WORKFLOW.example.md: the only "Orient:" slurp is line 737 (same fallback section).
  Both currently read exactly:  `1. Orient: ` + backtick `README.md`, `CLAUDE.md`, `docs/`.
  Items 2–6 in each numbered list are UNCHANGED.

JIT replacement substance (RESEARCH §Code Examples lines 460–471; exact wording = executor's
discretion, but MUST be): read root AGENTS.md first → match module-ownership path-globs to the
issue's acceptance criteria → read ONLY linked nodes that match → grep/glob the rest
(`grep -r '<term>' crates/` / `git ls-files | grep`) → do NOT open docs/ wholesale, do NOT read
README.md unless a node links to it. NO CLAUDE.md reference.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend HARNESS-SPEC §7.3 convention bullet + §9 checklist line</name>
  <files>docs/HARNESS-SPEC.md</files>
  <read_first>
    - docs/HARNESS-SPEC.md (lines 328–346 for §7.3 bullet style + the §7.4 boundary; lines 416–442 for the §9 `- [ ]` checklist style and where REQUIRED items end / RECOMMENDED begins)
    - .planning/phases/05-repository-context-contract/05-RESEARCH.md (Pattern 7 — exact §7.3 bullet text and §9 line text, lines 324–343)
    - .planning/phases/05-repository-context-contract/05-PATTERNS.md (HARNESS-SPEC amendment section lines 231–270 — insertion points + bold-term / MUST-caps / parenthetical-back-reference conventions)
    - docs/CONTEXT-CONTRACT.md (confirm the link target path is correct)
  </read_first>
  <action>
    Edit `docs/HARNESS-SPEC.md` (D-02 — reference only, NOT the contract body):

    - In §7.3 (after the CI-gates bullet ending at line 339, before §7.4 at line 341), insert a new bullet: `- **Context graph:** The repo MUST maintain a hierarchical `AGENTS.md` doc-graph conforming to `docs/CONTEXT-CONTRACT.md`. The root `AGENTS.md` is the agent entry point; all node edits ride in the code PR under CODEOWNERS. See [`docs/CONTEXT-CONTRACT.md`](docs/CONTEXT-CONTRACT.md) for the full contract.` Match the existing bullet style: bold key term, MUST in caps, markdown link.
    - In §9 (before the `*(RECOMMENDED)*` line at 438, with the REQUIRED items), append: `- [ ] A root `AGENTS.md` exists and conforms to `docs/CONTEXT-CONTRACT.md`; CODEOWNERS gates all `**/AGENTS.md` edits. (§7.3)` — `- [ ]` bullet, `(§7.3)` back-reference, consistent with existing checklist lines.

    Exact wording is at the executor's discretion within these bounds; the contract body must NOT be duplicated into HARNESS-SPEC (D-01/D-02).
  </action>
  <verify>
    <automated>[ "$(grep -c 'CONTEXT-CONTRACT' docs/HARNESS-SPEC.md)" -ge 2 ] && grep -q 'Context graph' docs/HARNESS-SPEC.md && grep -q '\*\*/AGENTS.md' docs/HARNESS-SPEC.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'CONTEXT-CONTRACT' docs/HARNESS-SPEC.md` ≥ 2 (the §7.3 bullet's prose mention + markdown link, and/or the §9 line).
    - The §7.3 Context-graph bullet appears between the CI-gates bullet and the §7.4 header, uses bold term + MUST, and markdown-links to `docs/CONTEXT-CONTRACT.md`.
    - The §9 checklist gains a `- [ ]` line referencing root `AGENTS.md` conformance + `**/AGENTS.md` CODEOWNERS gating, placed with the REQUIRED items (before the `*(RECOMMENDED)*` line), with a `(§7.3)` back-reference.
    - HARNESS-SPEC does NOT contain the contract body (no front-matter schema definition, no resolution algorithm prose copied in) — reference only.
  </acceptance_criteria>
  <done>
    HARNESS-SPEC §7.3 lists the Context Contract as a REQUIRED convention referencing `docs/CONTEXT-CONTRACT.md`, and §9 has the matching conformance-checklist line (SC-3).
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace the fallback Orient step with the JIT protocol in both WORKFLOW files</name>
  <files>docker/WORKFLOW.md, docker/WORKFLOW.example.md</files>
  <read_first>
    - docker/WORKFLOW.md (lines 480–505 — the full fallback "What to do" block; the only slurp is line 499; items 2–6 must stay unchanged)
    - docker/WORKFLOW.example.md (lines 720–743 — identical fallback block; slurp at line 737)
    - .planning/phases/05-repository-context-contract/05-RESEARCH.md (Pattern 6 + §Code Examples lines 451–471 — JIT replacement text; Open Question 1 recommendation: edit the fallback ONLY, since Todo/In Progress prompts have no orient step; Pitfall 4 — confirm CLAUDE.md appears ONLY at 499/737)
    - .planning/phases/05-repository-context-contract/05-PATTERNS.md (WORKFLOW.md fallback edit lines 274–317; WORKFLOW.example.md fallback edit lines 321–355; "Fallback Prompt List Item Style" lines 442–448 — lettered a./b./c./d. sub-list + a `Do NOT` warning paragraph)
  </read_first>
  <action>
    In BOTH `docker/WORKFLOW.md` (line 499) and `docker/WORKFLOW.example.md` (line 737), replace the single line `1. Orient: README.md, CLAUDE.md, docs/.` with the JIT orient protocol as item 1 of the numbered list. Substance (exact wording is the executor's discretion): a lettered sub-list — (a) read root `AGENTS.md`; (b) in its module-ownership table, find rows whose path-globs match files this issue's acceptance criteria mention or imply; (c) read ONLY the `AGENTS.md` nodes linked from those matching rows; (d) for anything not yet found, `grep -r '<term>' crates/` or `git ls-files | grep '<pattern>'` — followed by a `Do NOT` line: do not open `docs/` wholesale, do not read `README.md` unless a node links to it. The replacement MUST drop the `CLAUDE.md` reference entirely (D-03).

    Edit ONLY the fallback "What to do" item 1 in each file. Items 2–6 are unchanged. Do NOT add an orient preamble to the Todo or In Progress prompts (they have no orient step today; per RESEARCH Open Question 1, editing the fallback satisfies SC-4 / CTXGRAPH-02's "no blanket docs/ read" — and per Assumption A4 the bare slurp exists only in the fallback). Use identical JIT text in both files for parity.
  </action>
  <verify>
    <automated>[ "$(grep -c 'Orient.*docs/' docker/WORKFLOW.md docker/WORKFLOW.example.md 2>/dev/null | awk -F: '{s+=$2} END{print s+0}')" -eq 0 ] && [ "$(grep -c 'CLAUDE.md' docker/WORKFLOW.md docker/WORKFLOW.example.md 2>/dev/null | awk -F: '{s+=$2} END{print s+0}')" -eq 0 ] && [ "$(grep -c 'AGENTS.md' docker/WORKFLOW.md)" -ge 1 ] && [ "$(grep -c 'AGENTS.md' docker/WORKFLOW.example.md)" -ge 1 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Orient.*docs/" docker/WORKFLOW.md docker/WORKFLOW.example.md` → 0 hits (no blanket docs/ slurp remains).
    - `grep -n "CLAUDE.md" docker/WORKFLOW.md docker/WORKFLOW.example.md` → 0 hits (stale reference dropped, D-03).
    - `grep -c "AGENTS.md" docker/WORKFLOW.md` ≥ 1 AND `grep -c "AGENTS.md" docker/WORKFLOW.example.md` ≥ 1 (JIT protocol points to root AGENTS.md in both).
    - The JIT orient text is identical in both files (parity); items 2–6 of each fallback list are byte-unchanged from before the edit.
    - The replacement uses the lettered a./b./c./d. sub-list + a `Do NOT` warning, and the protocol order is: root AGENTS.md → acceptance-matching links → grep/glob → never blanket docs/.
  </acceptance_criteria>
  <done>
    The fallback Orient step in both WORKFLOW files uses the JIT read protocol pointing at root `AGENTS.md` with no blanket `docs/` slurp and no `CLAUDE.md` reference (SC-4 / CTXGRAPH-02).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Spec-doc + prompt-text editing only — no code, inputs, auth, or data flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | docker/WORKFLOW*.md (agent-instruction prompts) | mitigate | Prompt edits ride the gated PR under CODEOWNERS; human review confirms the JIT protocol is deterministic before merge (DEC-004). |
| T-05-09 | Information Disclosure | JIT protocol guidance (what agents read) | accept | The JIT protocol REDUCES context surface (narrower reads than the prior blanket docs/ slurp); no new disclosure risk introduced. |
| T-05-10 | (S/R/D/E) | n/a | accept | No external inputs, auth, or data flows; ASVS V2–V6 N/A per RESEARCH §Security Domain. |

No package-manager installs in this plan.
</threat_model>

<verification>
- `grep -c 'CONTEXT-CONTRACT' docs/HARNESS-SPEC.md` ≥ 2.
- `grep -n "Orient.*docs/" docker/WORKFLOW.md docker/WORKFLOW.example.md` → 0 hits.
- `grep -n "CLAUDE.md" docker/WORKFLOW.md docker/WORKFLOW.example.md` → 0 hits.
- `grep -c "AGENTS.md" docker/WORKFLOW.md` ≥ 1 and same for WORKFLOW.example.md.
- Manual: read the rewritten orient step in both files — an agent can deterministically pick the applicable node and follow only matching links.
</verification>

<success_criteria>
- SC-3 (§7 lists the Context Contract as a REQUIRED convention + §9 checklist line): satisfied by Task 1.
- SC-4 (every state prompt uses the JIT read protocol, no blanket docs/ read): satisfied by Task 2.
- D-02, D-03, D-04: spec wiring + dropped CLAUDE.md + JIT protocol all satisfied.
</success_criteria>

<output>
Create `.planning/phases/05-repository-context-contract/05-04-SUMMARY.md` when done.
</output>
