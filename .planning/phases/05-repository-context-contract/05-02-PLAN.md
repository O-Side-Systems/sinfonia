---
phase: 05-repository-context-contract
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - AGENTS.md
  - crates/sinfonia/AGENTS.md
  - crates/sinfonia-bridge/AGENTS.md
  - crates/sinfonia-tracker/AGENTS.md
  - docker/AGENTS.md
  - docs/AGENTS.md
  - .github/CODEOWNERS
autonomous: true
requirements: [CTXGRAPH-03]
must_haves:
  truths:
    - "Root AGENTS.md exists as the single agent entry point, carrying a four-column module-ownership table keyed path-globs → owners (D-03, D-05, SC-2 sample-node-validates)"
    - "Five area nodes exist (crates/sinfonia, crates/sinfonia-bridge, crates/sinfonia-tracker, docker, docs), each one-concern/one-screen and contract-valid (D-05)"
    - "Every node validates against docs/CONTEXT-CONTRACT.md §Conformance Checklist (six front-matter fields, ≤50 body lines, See also present) (SC-2)"
    - ".github/CODEOWNERS gates **/AGENTS.md edits and the node paths, mirroring the module-ownership tables (D-08)"
  artifacts:
    - path: "AGENTS.md"
      provides: "Root agent entry point + module-ownership index"
      contains: "## Module Ownership"
    - path: "crates/sinfonia/AGENTS.md"
      provides: "Orchestrator + agent-runner crate node"
      contains: "## Module Ownership"
    - path: "crates/sinfonia-bridge/AGENTS.md"
      provides: "CI-feedback bridge crate node"
      contains: "## Module Ownership"
    - path: "crates/sinfonia-tracker/AGENTS.md"
      provides: "Linear/Jira adapter crate node"
      contains: "## Module Ownership"
    - path: "docker/AGENTS.md"
      provides: "Docker topology + WORKFLOW templates area node"
      contains: "## Module Ownership"
    - path: "docs/AGENTS.md"
      provides: "Spec docs + proposals area node"
      contains: "## Module Ownership"
    - path: ".github/CODEOWNERS"
      provides: "Explicit gate on **/AGENTS.md + per-node-path ownership"
      contains: "**/AGENTS.md"
  key_links:
    - from: "AGENTS.md"
      to: "crates/*/AGENTS.md, docker/AGENTS.md, docs/AGENTS.md"
      via: "module-ownership table 'Don't Roll Your Own' column links + See also"
      pattern: "AGENTS.md"
    - from: ".github/CODEOWNERS"
      to: "AGENTS.md module-ownership path-globs"
      via: "per-area ownership entries mirror the node tables (CODEOWNERS authoritative)"
      pattern: "/crates/sinfonia"
---

<objective>
Full dogfood (D-05): author the six real `AGENTS.md` nodes for this repo — root + each crate (`crates/sinfonia`, `crates/sinfonia-bridge`, `crates/sinfonia-tracker`) + key dirs (`docker/`, `docs/`) — each contract-valid and ≤ one screen. Then extend `.github/CODEOWNERS` (D-08) to gate `**/AGENTS.md` and the node paths, mirroring the module-ownership tables.

Purpose: Prove the contract in practice and satisfy CTXGRAPH-03 / SC-2 ("a sample node validates against the contract") and D-03 (root AGENTS.md is the single agent entry point). Each node must pass the `docs/CONTEXT-CONTRACT.md` §Conformance Checklist authored in Plan 01.

Output: 6 `AGENTS.md` files + extended `.github/CODEOWNERS`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-repository-context-contract/05-CONTEXT.md
@.planning/phases/05-repository-context-contract/05-RESEARCH.md
@.planning/phases/05-repository-context-contract/05-PATTERNS.md

# The contract authored in Plan 01 — every node validates against its §Conformance Checklist.
@docs/CONTEXT-CONTRACT.md

# Ownership source the module-ownership tables mirror (append after the `*` catch-all).
@.github/CODEOWNERS

<interfaces>
<!-- Authoritative node templates from RESEARCH.md §Code Examples + PATTERNS.md.
     Front-matter field order is fixed: title, version, status, owners, last_verified_sha, derived_from. -->

Front-matter block (open every node with this YAML):
  ---
  title: "<name> — <one-line scope>"
  version: "1.0.0"
  status: active
  owners: ["@osidesys", "@leebrett"]
  last_verified_sha: "1a482ec"   # current HEAD short SHA; executor re-runs `git rev-parse --short HEAD` and uses the actual value
  derived_from: "<source doc, e.g. docs/SPEC.md §11.6>"
  ---

Root module-ownership table columns (four): Capability | Path-glob | Owned By | Don't Roll Your Own
Crate/area node table columns (four):       Capability | Path-glob | Owned By | Notes

CODEOWNERS append block (AFTER the existing `* @osidesys @leebrett` line, last-match-wins):
  **/AGENTS.md @osidesys @leebrett
  /crates/sinfonia/         @osidesys @leebrett
  /crates/sinfonia-bridge/  @osidesys @leebrett
  /crates/sinfonia-tracker/ @osidesys @leebrett
  /docker/                  @osidesys @leebrett
  /docs/                    @osidesys @leebrett
  /templates/               @osidesys @leebrett
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author root AGENTS.md + extend .github/CODEOWNERS</name>
  <files>AGENTS.md, .github/CODEOWNERS</files>
  <read_first>
    - docs/CONTEXT-CONTRACT.md (§Node front-matter schema, §Sizing, §Conformance Checklist — the node MUST pass it)
    - .planning/phases/05-repository-context-contract/05-RESEARCH.md (§Code Examples root-node body at lines 413–445; Pattern 5 CODEOWNERS extension at lines 477–496; Pitfall 1 "root node is an index, not prose"; Pitfall 2 CODEOWNERS last-match-wins ordering)
    - .planning/phases/05-repository-context-contract/05-PATTERNS.md (root-node body pattern lines 115–142; CODEOWNERS append pattern lines 359–384)
    - .github/CODEOWNERS (the existing 3 lines — append only, do NOT reorder)
    - README.md (for the one-sentence project orientation — root stays an index, README stays human-facing per D-03)
  </read_first>
  <action>
    Create `AGENTS.md` at repo root as the single agent entry point (D-03). Open with the six-field front-matter block (set `last_verified_sha` to the output of `git rev-parse --short HEAD`; `derived_from: "README.md, docs/SPEC.md"`). Body: one orientation sentence (Sinfonia is a polling orchestrator over Linear/Jira; three Rust crates), then a `## Module Ownership` four-column table (Capability | Path-glob | Owned By | Don't Roll Your Own) using crate-level globs (`crates/sinfonia/`, `crates/sinfonia-bridge/`, `crates/sinfonia-tracker/`, `docker/`, `docs/`) with the "Don't Roll Your Own" column linking to each area node's AGENTS.md, then a `## See also` link list (docs/SPEC.md, docs/HARNESS-SPEC.md, docs/CONTEXT-CONTRACT.md). Keep the body ≤ 50 lines — index + table only, NO architecture prose (Pitfall 1).

    Then extend `.github/CODEOWNERS`: APPEND (do not reorder) after the existing `* @osidesys @leebrett` line the `**/AGENTS.md` gate plus per-area ownership entries for /crates/sinfonia/, /crates/sinfonia-bridge/, /crates/sinfonia-tracker/, /docker/, /docs/, /templates/ — each `@osidesys @leebrett`, grouped under comment headers, last-match-wins ordering (Pitfall 2). These path-globs must match the root module-ownership table.
  </action>
  <verify>
    <automated>test -f AGENTS.md && grep -q "## Module Ownership" AGENTS.md && grep -q "## See also" AGENTS.md && [ "$(grep -v '^#' AGENTS.md | grep -c '')" -ge 1 ] && BODY=$(awk 'NR>1 && /^---$/{f=1; next} f' AGENTS.md | grep -c '') && [ "$BODY" -le 50 ] && grep -q '\*\*/AGENTS.md' .github/CODEOWNERS && grep -q '/templates/' .github/CODEOWNERS && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `ls AGENTS.md` succeeds; front-matter has all six fields (title, version, status, owners, last_verified_sha, derived_from).
    - `last_verified_sha` equals the current HEAD short SHA (`git rev-parse --short HEAD`) and is present in `main` ancestry.
    - Root body (excluding front-matter) is ≤ 50 rendered lines (`awk` body extraction above).
    - Root contains a `## Module Ownership` four-column table and a `## See also` list; no multi-paragraph architecture prose (Pitfall 1).
    - `.github/CODEOWNERS` contains `**/AGENTS.md` and all seven per-area entries, appended AFTER the `*` catch-all (last-match-wins); the original 3 lines are unchanged and still first.
    - Every path-glob in the root module-ownership table matches a pattern present in `.github/CODEOWNERS` (conformance-checklist item).
  </acceptance_criteria>
  <done>
    Root `AGENTS.md` exists as a contract-valid index node ≤ one screen, and `.github/CODEOWNERS` gates `**/AGENTS.md` + the node paths with the table-mirroring per-area entries.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author the five area nodes (3 crates + docker + docs)</name>
  <files>crates/sinfonia/AGENTS.md, crates/sinfonia-bridge/AGENTS.md, crates/sinfonia-tracker/AGENTS.md, docker/AGENTS.md, docs/AGENTS.md</files>
  <read_first>
    - docs/CONTEXT-CONTRACT.md (§Conformance Checklist — each of the five nodes MUST pass it; §Sizing ≤50 body lines)
    - .planning/phases/05-repository-context-contract/05-PATTERNS.md (per-crate node skeleton lines 144–171; Pitfall 7 "dogfood nodes must not copy-paste — name real source files per area")
    - .planning/phases/05-repository-context-contract/05-RESEARCH.md (Pattern 1 front-matter; Pattern 3 sizing; Open Question 2 — coarse globs at root, fine-grained sub-module globs in area nodes)
    - crates/sinfonia/src/ , crates/sinfonia-bridge/src/ , crates/sinfonia-tracker/src/ (glob the actual sub-module directories so each node's table names REAL paths — e.g. orchestrator/dispatch.rs — not placeholders)
    - .github/CODEOWNERS (the per-area entries the node tables must be consistent with — written in Task 1)
  </read_first>
  <action>
    Create five contract-valid `AGENTS.md` nodes, one per area, each opening with the six-field front-matter (re-use the HEAD short SHA for `last_verified_sha`; set `derived_from` to the relevant `docs/SPEC.md §N` for each):

    - `crates/sinfonia/AGENTS.md` — scope: orchestrator + agent runners. Module-ownership sub-table over real sub-modules under `crates/sinfonia/src/` (e.g. `orchestrator/`, `agent/`). Name a real coupling invariant (e.g. don't edit `dispatch.rs` without checking its coupled module). `See also` → docs/SPEC.md relevant §, root ../../AGENTS.md.
    - `crates/sinfonia-bridge/AGENTS.md` — scope: CI-to-issue feedback bridge. Sub-table over real `crates/sinfonia-bridge/src/` modules; `derived_from` references docs/SPEC.md §11.6 + proposal 0001. `See also` → BRIDGE.md / docs/SPEC.md, root.
    - `crates/sinfonia-tracker/AGENTS.md` — scope: Linear/Jira adapters. Sub-table over real `crates/sinfonia-tracker/src/` modules. `See also` → docs/SPEC.md §8, root.
    - `docker/AGENTS.md` — scope: docker topology + WORKFLOW templates. Sub-table over real `docker/` paths (WORKFLOW.md, WORKFLOW.example.md, etc.). `See also` → docs/HARNESS-SPEC.md, root.
    - `docs/AGENTS.md` — scope: spec docs + proposals; explicitly NOT an agent entry point (root AGENTS.md is). Sub-table over docs/ areas. `See also` → docs/CONTEXT-CONTRACT.md, docs/HARNESS-SPEC.md, root.

    Each node ≤ 50 body lines, one concern, four-column table (Capability | Path-glob | Owned By | Notes), `## See also` list. Every sub-table path-glob must be covered by a `.github/CODEOWNERS` pattern (the `/crates/.../` and `/docker/`, `/docs/` entries from Task 1). Do NOT copy-paste identical bodies — each names real, area-specific files (Pitfall 7).
  </action>
  <verify>
    <automated>for n in crates/sinfonia/AGENTS.md crates/sinfonia-bridge/AGENTS.md crates/sinfonia-tracker/AGENTS.md docker/AGENTS.md docs/AGENTS.md; do test -f "$n" || { echo "MISSING $n"; exit 1; }; for f in title version status owners last_verified_sha derived_from; do grep -q "$f" "$n" || { echo "$n missing $f"; exit 1; }; done; grep -q "## See also" "$n" || { echo "$n missing See also"; exit 1; }; BODY=$(awk 'NR>1 && /^---$/{ff=1; next} ff' "$n" | grep -c ''); [ "$BODY" -le 50 ] || { echo "$n body $BODY > 50"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All five files exist: `crates/sinfonia/AGENTS.md`, `crates/sinfonia-bridge/AGENTS.md`, `crates/sinfonia-tracker/AGENTS.md`, `docker/AGENTS.md`, `docs/AGENTS.md`.
    - Each node has all six front-matter fields, a `## See also` list, and a body ≤ 50 rendered lines.
    - Each node's module-ownership sub-table names REAL source paths in its area (verified against the actual `src/` / `docker/` / `docs/` directory contents), not placeholder strings — no two nodes are byte-identical bodies (Pitfall 7).
    - Every sub-table path-glob is covered by a `.github/CODEOWNERS` pattern.
    - `docs/AGENTS.md` states it is NOT the agent entry point (root `AGENTS.md` is) per D-03.
  </acceptance_criteria>
  <done>
    Five area `AGENTS.md` nodes exist, each contract-valid (six fields, ≤ one screen, See also present), naming real per-area paths consistent with CODEOWNERS — the full dogfood (D-05) of root + 5 areas is complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Documentation/config authoring only — no code, inputs, auth, or data flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering / Elevation | `.github/CODEOWNERS` (the human-review gate itself) | mitigate | CODEOWNERS edits are themselves gated by the existing `* @osidesys @leebrett` catch-all (CODEOWNERS lives under repo root); the new `**/AGENTS.md` line makes the doc-node write gate explicit (DEC-004). |
| T-05-04 | Tampering | AGENTS.md nodes (agent-consumed guidance) | mitigate | Nodes are human-authored surgical diffs reviewed under CODEOWNERS; no autonomous mutation (DEC-004). |
| T-05-05 | (S/R/I/D) | n/a | accept | No external inputs, auth, or data flows; ASVS V2–V6 N/A per RESEARCH §Security Domain. |

No package-manager installs in this plan.
</threat_model>

<verification>
- `find . -maxdepth 3 -name AGENTS.md -not -path './node_modules/*'` lists exactly 6 nodes (root + 5 areas).
- Per-wave: walk `docs/CONTEXT-CONTRACT.md §Conformance Checklist` for all 6 nodes.
- `grep -c '\*\*/AGENTS.md' .github/CODEOWNERS` ≥ 1; original `* @osidesys @leebrett` still present and first.
</verification>

<success_criteria>
- SC-2 ("a sample node validates against the contract"): satisfied — all 6 dogfood nodes pass the §Conformance Checklist.
- D-03, D-05, D-07, D-08: root entry point + full dogfood + per-crate/dir granularity + CODEOWNERS mirroring all satisfied.
</success_criteria>

<output>
Create `.planning/phases/05-repository-context-contract/05-02-SUMMARY.md` when done.
</output>
