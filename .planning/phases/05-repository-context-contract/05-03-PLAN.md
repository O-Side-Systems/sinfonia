---
phase: 05-repository-context-contract
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - templates/AGENTS.md
  - templates/CODEOWNERS
autonomous: true
requirements: [CTXGRAPH-03]
must_haves:
  truths:
    - "templates/AGENTS.md ships as a downstream-repo starter with the module-ownership table and the six-field front-matter, rendering <1 screen (D-06, SC-2)"
    - "templates/CODEOWNERS ships as a downstream-repo starter mirroring the AGENTS.md gate pattern with last-match-wins ordering (D-06)"
    - "Both templates use <!-- replace: ... --> markers only (no prose), with fill-in guidance living in docs/CONTEXT-CONTRACT.md (Pitfall 6)"
  artifacts:
    - path: "templates/AGENTS.md"
      provides: "Downstream-repo AGENTS.md starter with module-ownership table"
      contains: "## Module Ownership"
    - path: "templates/CODEOWNERS"
      provides: "Downstream-repo CODEOWNERS starter with the **/AGENTS.md gate"
      contains: "**/AGENTS.md"
  key_links:
    - from: "templates/AGENTS.md"
      to: "docs/CONTEXT-CONTRACT.md"
      via: "front-matter schema + sizing conform to the contract; fill-in guidance lives in the contract"
      pattern: "version|owners|last_verified_sha"
    - from: "templates/CODEOWNERS"
      to: "templates/AGENTS.md"
      via: "**/AGENTS.md gate mirrors the node module-ownership pattern"
      pattern: "AGENTS.md"
---

<objective>
Ship the downstream-repo templates (D-06): `templates/AGENTS.md` (with module-ownership table + six-field front-matter) and `templates/CODEOWNERS` (with the `**/AGENTS.md` gate). Both conform to `docs/CONTEXT-CONTRACT.md` and render as <1-screen nodes.

Purpose: Satisfy CTXGRAPH-03 / SC-2 ("templates/AGENTS.md and templates/CODEOWNERS ship and render as <1-screen nodes"). These are starters other repos copy — guidance for filling them in lives in the contract, not in the files (Pitfall 6).

Output: `templates/AGENTS.md` + `templates/CODEOWNERS` (creating the `templates/` dir).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-repository-context-contract/05-CONTEXT.md
@.planning/phases/05-repository-context-contract/05-RESEARCH.md
@.planning/phases/05-repository-context-contract/05-PATTERNS.md

# The contract the templates conform to (front-matter schema, sizing, fill-in guidance home).
@docs/CONTEXT-CONTRACT.md

# Exact-template analog for templates/CODEOWNERS.
@.github/CODEOWNERS

<interfaces>
<!-- Template front-matter (placeholder values, status: draft) — RESEARCH.md lines 180–189, PATTERNS.md lines 179–195 -->
  ---
  title: "<repo name> — Agent Entry Point"
  version: "1.0.0"
  status: draft
  owners: ["@<owner1>"]
  last_verified_sha: "<7-char git SHA of last verified commit>"
  derived_from: "README.md"
  ---

Module-ownership table columns: Capability | Path-glob | Owned By | Don't Roll Your Own
Replace-marker style: <!-- replace: ... --> (NO prose explanations; Pitfall 6)

templates/CODEOWNERS skeleton (PATTERNS.md lines 210–227): `* @<owner1> @<owner2>` then,
AFTER it, `**/AGENTS.md @<owner1> @<owner2>` and per-area `/crates/<crate-N>/`, `/docker/`,
`/docs/`, `/templates/` entries — all using <!-- replace: ... --> markers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author templates/AGENTS.md and templates/CODEOWNERS</name>
  <files>templates/AGENTS.md, templates/CODEOWNERS</files>
  <read_first>
    - docs/CONTEXT-CONTRACT.md (§Node front-matter schema + §Sizing — templates must conform; the contract is where fill-in guidance lives, NOT the template file)
    - .planning/phases/05-repository-context-contract/05-RESEARCH.md (§Code Examples; Pitfall 6 "template node too large — use replace-markers only, no prose")
    - .planning/phases/05-repository-context-contract/05-PATTERNS.md (templates/AGENTS.md section lines 175–195; templates/CODEOWNERS section lines 199–227)
    - .github/CODEOWNERS (the exact-template analog content)
    - AGENTS.md (root, authored in Plan 02) — the real-node shape the template generalizes (do not copy real owners/paths into the template; use replace-markers)
  </read_first>
  <action>
    Create the `templates/` directory and two files:

    - `templates/AGENTS.md` — open with the six-field front-matter using placeholder values (`title: "<repo name> — Agent Entry Point"`, `version: "1.0.0"`, `status: draft`, `owners: ["@<owner1>"]`, `last_verified_sha: "<7-char git SHA of last verified commit>"`, `derived_from: "README.md"`). Body: one `<!-- replace: one-sentence project orientation -->` line, a `## Module Ownership` four-column table (Capability | Path-glob | Owned By | Don't Roll Your Own) whose rows are a single `<!-- replace: add one row per shared capability -->` placeholder, and a `## See also` list with `<!-- replace: link to your spec docs + this template's source contract -->`. Use `<!-- replace: ... -->` markers ONLY — no prose explanation (Pitfall 6); the actual fill-in guidance lives in `docs/CONTEXT-CONTRACT.md`. The file MUST render ≤ 58 total lines (≤ 50 body).

    - `templates/CODEOWNERS` — mirror `.github/CODEOWNERS` with placeholders: header comment + `* @<owner1> @<owner2>`, then AFTER it (last-match-wins) `**/AGENTS.md @<owner1> @<owner2>` and per-area entries `/crates/<crate-1>/`, `/crates/<crate-2>/`, `/docker/`, `/docs/`, `/templates/` each `@<owner1> @<owner2>`, grouped under comment headers. Use `<!-- replace: ... -->`-style guidance only where a value must be filled (owner handles, crate names). The `**/AGENTS.md` line MUST come after the `*` catch-all.
  </action>
  <verify>
    <automated>test -f templates/AGENTS.md && test -f templates/CODEOWNERS && grep -q "## Module Ownership" templates/AGENTS.md && grep -q "replace:" templates/AGENTS.md && [ "$(grep -c '' templates/AGENTS.md)" -le 58 ] && grep -q '\*\*/AGENTS.md' templates/CODEOWNERS && AGI=$(grep -n '\* @' templates/CODEOWNERS | head -1 | cut -d: -f1) && AGN=$(grep -n '\*\*/AGENTS.md' templates/CODEOWNERS | head -1 | cut -d: -f1) && [ "$AGN" -gt "$AGI" ] && for f in title version status owners last_verified_sha derived_from; do grep -q "$f" templates/AGENTS.md || { echo "missing $f"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `ls templates/AGENTS.md templates/CODEOWNERS` both succeed.
    - `wc -l templates/AGENTS.md` ≤ 58 (the <1-screen sizing gate from SC-2 / Pitfall 6).
    - `templates/AGENTS.md` has all six front-matter fields (with placeholder values, `status: draft`), a `## Module Ownership` four-column table, and a `## See also` list — all using `<!-- replace: ... -->` markers, NO prose paragraphs (Pitfall 6).
    - `templates/CODEOWNERS` contains `**/AGENTS.md` AFTER the `* @<owner...>` catch-all (last-match-wins ordering verified by line numbers), plus the per-area placeholder entries.
    - Neither template contains real owner handles or real sinfonia crate paths — only placeholders (these are downstream starters).
  </acceptance_criteria>
  <done>
    `templates/AGENTS.md` and `templates/CODEOWNERS` exist as contract-conforming, <1-screen, placeholder-only downstream starters with the module-ownership table and `**/AGENTS.md` gate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Template-file authoring only — no code, inputs, auth, or data flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Tampering | templates/* (copied into downstream repos) | mitigate | Templates ship placeholder-only with `status: draft`; downstream adopters review on copy; this repo's templates ride the gated PR under CODEOWNERS (DEC-004). |
| T-05-07 | (S/R/I/D/E) | n/a | accept | No code, inputs, auth, or data flows; ASVS V2–V6 N/A per RESEARCH §Security Domain. |

No package-manager installs in this plan.
</threat_model>

<verification>
- `wc -l templates/AGENTS.md` ≤ 58 and `wc -l templates/CODEOWNERS` (small, ≤ ~20).
- Manual: confirm the templates would pass `docs/CONTEXT-CONTRACT.md §Conformance Checklist` once placeholders are filled.
</verification>

<success_criteria>
- SC-2 (templates ship and render as <1-screen nodes): satisfied by Task 1.
- D-06 (templates ship for downstream repos): satisfied.
</success_criteria>

<output>
Create `.planning/phases/05-repository-context-contract/05-03-SUMMARY.md` when done.
</output>
