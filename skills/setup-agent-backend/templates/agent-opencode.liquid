agent:
  provider: opencode
  command: opencode
  # OpenCode routes internally to 75+ providers — pick one via
  # `opencode auth login` and confirm the active model with
  # `opencode model show`. See docs/v0.3-plan/02-opencode-VERIFY.md.
